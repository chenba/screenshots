-- Records using the new enum values from 22-23 must be updated or deleted in
-- order to update the enum type. For that reason, we refrain from removing the
-- enum values in here.

DROP TABLE watchdog_submissions;
